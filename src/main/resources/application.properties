spring.application.name=e-Bankati

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/bankati_db
spring.datasource.username=${DB_Username}
spring.datasource.password=${DB_Password}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
#logging.level.org.hibernate=DEBUG
#logging.level.org.springframework=DEBUG

# Liquibase Configuration
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.yaml
spring.liquibase.enabled=true
logging.level.liquibase=DEBUG

# Optimisations pour les performances
spring.jpa.properties.hibernate.jdbc.batch_size=100
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# Configuration du pool de connexions
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.idle-timeout=300000

# Logs
logging.level.org.projects.eBankati=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.root=INFO

# Server
server.error.include-message=always
server.error.include-binding-errors=always

# Security
logging.level.org.springframework.security=trace
spring.security.user.name=omario
spring.security.user.password=omario

# JWT Configuration
app.secret-key=3456789N9D865BF434560986U234780976T23123456765456R7809
app.expiration-time=400000
